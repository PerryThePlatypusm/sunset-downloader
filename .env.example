# Backend Configuration
# Set this when running a self-hosted backend on a different server
# Examples:
#   - Local: http://localhost:3000
#   - VPS: https://your-domain.com
#   - Docker: http://backend-container:3000
# Leave empty to use same-origin (Netlify Functions on same domain)
VITE_API_URL=

# Node Environment
# Use 'production' for production deployments
NODE_ENV=development

# Server Port
# Only needed when running backend locally
PORT=3000

# Custom Ping Message (optional)
# Response message for /api/ping endpoint
PING_MESSAGE=Backend online
